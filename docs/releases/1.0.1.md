# 말씨 1.0.1 출시노트

> Play Console 복붙용 + 내부 기록. 스토어에는 아래 `복붙용` 블록만 올린다.

## 복붙용 (스토어)

```
말씨 1.0.1입니다.

• 다음 성장까지 남은시간을 씨앗 아래 타이머로 표시
• 오늘의 씨앗은 14시까지 심기 가능, 13시에 마감 리마인드
• 열매가 완성되면 알림으로 알려드려요
• 앱 아이콘 교체
```

## 포함 내용

| PR | 이슈 | 내용 |
|----|------|------|
| #143 | #138 | 다음 성장까지 남은시간 표시 (HH:MM 타이머, #148에서 UI 개선) |
| #144 | #140 | 씨앗 완성 시 알림 (inexact, 탭 시 말씨 탭 이동) |
| #142 | — | 앱 아이콘 v2 (Android·iOS) |
| #146 | — | Firebase 의존성 제거 (사용자 노출 없음) |
| #148 | #138 | 타이머 UI 개선 (씨앗 아래 배치, 에셋 1.2x) |
| #155 | #147 | 오늘의 씨앗 14시 마감 + 13시 리마인드 |

## 검증

- `flutter analyze`: No issues found
- `flutter test`: 172개 전부 통과
- `flutter build appbundle --release` 성공

## 출시 전 체크

- [ ] `pubspec.yaml` 버전 확인 (현재 `1.0.0+1` — 스토어에 올릴 버전코드로 맞는지 확인)
- [ ] `release/1.0.1` → `main` 머지 확인
- [ ] Play Console에 위 `복붙용` 텍스트 등록
