# 2026-09-16 작업 기록

## 세션 1 (1.0.4 추가 3건 — 해설·식물·버튼)

### 개요
개발자 요청 3건(명언 해설·식물 축소·버튼 통일)을 이슈 분리 → 기능별 PR 3개로 작성.
단일 PR 요청이었으나 확인 결과 3개 PR 분리로 확정. 전부 `base=release/1.0.4`.

### 한 일
- 이슈 3건 신규: #216(해설) #217(식물) #218(버튼)
- PR #219 `feat/plant-size-restore` (#217):
  #160 확대 전(1x) 원복 — 성장 340→170·완성 300→150, 4x 에셋 그대로.
  `seed_test.dart` 170·150 회귀 포함.
- PR #220 `feat/settings-button-size` (#218):
  설정 화면 모드 `SegmentedButton`에 textStyle·minimumSize·padding 명시 고정.
  직접 측정 결과 기본 크기는 동일했으나 테마 기본값 의존 제거.
  라이트/다크 동일 크기 회귀 포함.
- PR #221 `feat/quote-explanation` (#216):
  `Quote.explanation` + `Fruit.explanation` 스냅샷 + `QuoteAssets` 파싱 +
  번들 159건 전수 해설 + 말씨탭·후기카드(작성/읽기)·정원 상세 표시
  (빈값 숨김, `keepWordsTogether` 적용). `model_spec.md` §4.1/§4.9,
  `feature_spec.md` §4 갱신.

### 검증
- 각 PR `flutter analyze` clean + `flutter test` 통과 (최종 239개).

### 이슈·PR
| 이슈 | PR | 내용 | 상태 |
|------|----|------|------|
| #216 | #221 | 명언 해설 159건 + explanation 필드·UI | 리뷰 대기 |
| #217 | #219 | 식물 1x 원복 | 리뷰 대기 |
| #218 | #220 | 설정 버튼 크기 통일 | 리뷰 대기 |

## 세션 2 (#152 위키 철학자 50선)

### 개요
남은 오픈 이슈 개발 착수. 1.0.4 마일스톤(#152·#139) 중 #152부터.
번들 159건이라 이슈 원문(108건 기준) 전제 변경 → 범위 재확정 후 진행.

### 한 일
- 후보지 조사: 우리말샘 속담 2차(표제어 10,769) vs 위키 잔여 648건 vs 고전/어록.
  철학자 문의 → 위키 잔여에 니체12·러셀17·맹자7·장자7 확인,
  시중 명언 사이트/책은 라이선스 불명확이라 비추천.
- 선정 기준 확정 (개발자): 철학자·대중 친숙 인물만 (김진왕 등 무명 제외),
  공감 소재만 (죽음·전쟁·차별·성적·극단·사상 제외, #180 82자·한자 동일).
  니체·러셀은 분량 초과·3자 서술이라 제외.
- PR #222 `feat/quote-philosophers-50` (#152):
  위키 특선 49건 + 한국어 위키인용집 쇼펜하우어 페이지 직접 발췌 2건
  (동일 CC BY-SA 4.0, 번역 불필요) = 51건 추가, 번들 210건 (위키 122 + 속담 88).
  신규 전수 해설 포함. 단축 6건(`full` 보존, #123 선례),
  에디슨 오타 1건 직접 수정 (#180 선례).
  `classify_quotes.py`에 KEEP·OVERRIDES·SHORTEN 반영 +
  `ead400915b39` SHORTEN 키 오기재 정정 (나폴레옹 → 아인슈타인).
  `model_spec.md` §4.11 수치 갱신.

### 검증
- `flutter analyze`: No issues found / `flutter test`: 231개 통과.
- 파이프라인 정합: 49건 전수 재현 검증 (테마 일치, `--report-only` 통과).
- 원본 대조 테스트에 쇼펜하우어 예외 추가 (md5 id, 속담과 동일 방식).

### 이슈·PR
| 이슈 | PR | 내용 | 상태 |
|------|----|------|------|
| #152 | #222 | 위키 철학자 51선 (번들 210건) | 리뷰 대기 |

### 주요 결정 사항
- #221 ↔ #222 머지 순서: 같은 `quotes.json`·테스트를 건드리므로,
  한쪽 머지 후 다른 쪽이 `release/1.0.4`에 리베이스 (충돌은 `explanation` 키 수준).
- `docs/releases/1.0.4.md` stub은 미갱신 — 4개 PR 머지 후 릴리스 준비 시점에 일괄 확정.
- 파이프라인 재생성(`classify_quotes.py` 실행)은 범위 외
  (속담·해설이 스크립트 출력에 없어 재생성 시 소실 — 별도 이슈 필요).

## 잔여 작업
- PR 4건 (#219 #220 #221 #222) 개발자 리뷰·머지 (LLM 자율 머지 금지).
- #222 머지 시 #221(또는 역순) 리베이스 1회 필요.
- #139 위젯 (범위 확정 필요) · #129 직접 작성 · #153 별점 유도 (v1.1.0).
- `docs/releases/1.0.4.md` 출시노트 확정 (PR 머지 후).

## 미커밋 잔여물
- `.dart_tool/`, `macos/...Registrant.swift` (환경 부산물, unstaged 유지).
- `assets/docs/proverbs.csv`, `docs/quotes/proverbs_review.md` (untracked, 손대지 않음).
