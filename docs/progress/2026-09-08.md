# 2026-09-08 작업 기록

> 9월 7일 기록 파일이 없으므로 9/7 오후~9/8 새벽 작업을 세션 1에 합산한다.
> 저녁에 작업하면 아래에 `## 세션 2 (저녁)`을 추가한다.

## 세션 1 (9/7 오후~9/8 새벽)

### 개요
온보딩(#130) 머지 후 스크린샷·지원 페이지·iOS 빌드 기반 작업. 테스트 135 → 153개,
analyze clean으로 종료. 전량 로컬 머지 후 `main` 푸시.

### 이슈·PR
| 이슈 | PR | 내용 | 상태 |
|------|----|------|------|
| #130 | #131 | 온보딩 5페이지 + 첫 실행 게이트 + 도움말 다시 보기 (전일 작업분) | 머지됨 |
| — | #131 (추가 커밋) | 스크린샷용 디버그 UI 숨김 (`HIDE_DEBUG_UI` + 설정 스위치 + 리본 숨김), 온보딩 스크린샷 8장·개명·페이지 연결·제목 한 줄 처리 | 머지됨 |
| — | #131 (추가 커밋) | `support/index.html` 지원 페이지 → `/support/` 경로로 이동 | 머지됨 |
| — | #132 | `support/privacy.html` 개인정보 처리방침 신규 + 지원 페이지 연결 (제3자 SDK 표현 수정, 이메일·푸터 통일) | 머지됨 |
| — | #133 | `ios/` 플랫폼 폴더 추적 시작 (42개, Pods 등 산출물 제외) | 머지됨 |
| — | #134 | `ci_scripts/ci_post_clone.sh` (Flutter 설치·pub get·pod install) | 머지됨 |
| — | #135 | `ci_scripts/` → `ios/ci_scripts/` 이동 (Xcode Cloud는 xcodeproj 옆만 인식) | 열림 |
| — | #136 | iOS 앱 아이콘 교체 (원본 `assets/icon/app_icon.png`, 15 사이즈, alpha 제거, 여백 축소→그라데이션 원본 전환) | 열림 |

### 주요 결정 사항
- 스크린샷 모드 2종: 실행 옵션(`--dart-define=HIDE_DEBUG_UI=true`) + 설정 탭 스위치(디버그 전용, `DebugUiProvider`). 디버그 리본은 항상 숨김.
- 온보딩 스크린샷 8장: `seed_locked/growing`, `fruit_complete`, `review_empty/filled`, `archive_garden`, `settings`, `settings_seed_time`. `assets/images/`만으로는 하위 폴더가 번들에 안 들어가서 `assets/images/screenshots/` 명시 등록 + `screenshot_assets_test.dart` 회귀 테스트.
- Pages 지원 경로는 `support/index.html` (`/malssi/support/`). Pages는 머지 후 Settings에서 직접 켜야 함 (아직 안 켬).
- `ci_scripts` 위치는 저장소 루트가 아니라 `ios/` 옆. 루트에 두면 Cloud가 못 찾음.
- 앱 아이콘: 크롭 실험 후 그라데이션 풀블리드 원본 채택. 소스는 `assets/icon/`에 보관 (pubspec 등록 안 함, 앱 용량 제외).
- 실기기 서명 오류는 계정 기기 미등록이 원인. UDID는 USB 연결 후 Finder 일련번호 클릭 또는 Xcode Devices에서 확인 (Wi-Fi 연결만으로는 불가).
- Xcode Cloud export 실패(`Development/Ad Hoc`)는 서명 문제로, 경고 7개(서드파티 deprecation 등)는 무해.

### 검증
- `flutter analyze`: No issues found.
- `flutter test`: 153개 전부 통과.
- `flutter build ios --simulator` 성공, 시뮬레이터 홈 화면에서 커스텀 아이콘 표시 확인 (캐시 꼬이면 uninstall·erase 후 재설치).
- `main` = `82757dc` (feat/app-icon 머지).

### 잔여 작업
- #129 그날의 명언 직접 작성 (미착수).
- PR #135·#136 머지 확인 (GitHub 머지 가능 계산이 느리면 로컬 머지).
- Pages 켜기 (Settings → Pages → main / root) 후 `/support/` 접속 확인.
- TestFlight 업로드 시 서명·기기 등록 마저 처리.
- 미커밋 잔여물: `.dart_tool/`, `.metadata`, `analysis_options.yaml`(ios 제외 추가),
  `macos/...Registrant.swift`(shared_preferences 등록), `pubspec.lock`(platform 3.2.0),
  `ios/.../project.pbxproj`(Xcode 자동 주석·빈배열 정리) — 전부 환경 부산물로 unstaged 유지.
  `ios/` (실기기 빌드용 로컬 폴더)도 미커밋.

## 세션 2 (저녁)

### 개요
Play Console용 첫 앱 번들(AAB) 빌드. `android/` 플랫폼 폴더 복원 + 업로드 키 발급·서명 설정 +
`flutter_local_notifications` 16→22 마이그레이션. `flutter analyze` clean,
`flutter test` 153개 통과, `app-release.aab`(53MB) 빌드 성공.

### 한 일
- `flutter create --platforms=android --org com.leaf`로 `android/` 복원
  (applicationId `com.leaf.malssi`, iOS 번들 ID와 동일).
- 업로드 키 신규 발급: `android/app/upload-keystore.jks` (alias `upload`, ~2054).
  `android/key.properties` + `build.gradle.kts` release signing 연결.
  키 파일은 git 제외(`.gitignore` + `android/.gitignore`), 별도 백업 필요.
- `flutter_local_notifications` `^16.1.0` → `^22.3.0`
  (`timezone` `^0.9.4` → `^0.11.1` 동반 상향).
  구버전이 새 Android SDK에서 컴파일 실패
  (`bigLargeIcon(null)` ambiguous)해서 상향이 필수였음.
- `lib/core/services/notification_service.dart`를 v22 named-parameter API로 마이그레이션
  (`initialize(settings:)`, `zonedSchedule(id:/scheduledDate:/androidScheduleMode:)`,
  `show(id:/notificationDetails:)`, `cancel(id:)`, iOS 설정 추가).
  공개 API 시그니처는 그대로라 `architecture_spec.md` §2.3 유효.
- `android/app/build.gradle.kts`에 desugaring·multiDex 추가 (플러그인 요구사항).
- `analysis_options.yaml`에 `ios/**`·`android/**` analyzer 제외
  (`flutter create` 자동 추가, 플랫폼 폴더 추적에 맞춰 커밋).

### 검증
- `flutter analyze`: No issues found.
- `flutter test`: 153개 전부 통과.
- `flutter build appbundle --release` 성공:
  `build/app/outputs/bundle/release/app-release.aab` (53MB, `jarsigner -verify` 통과).
- SHA-256: `AC:F4:1E:FA:66:B1:DE:1C:92:8E:6E:41:26:16:6F:84:98:73:15:A0:C4:70:F8:B6:8F:1A:0F:E7:E3:0A:25:E5`.

### 잔여 작업
- 업로드 키(`upload-keystore.jks` + 비밀번호) 외부 백업 (분실 시 업데이트 불가).
- Play Console에 앱 생성 후 `.aab` 업로드 (패키지명 `com.leaf.malssi` 일치 필수).
- 미커밋 잔여물(세션 1에서 이어짐): `.dart_tool/`,
  `ios/.../project.pbxproj`(Xcode 자동 정리), `macos/...Registrant.swift` —
  환경 부산물로 unstaged 유지. `.metadata`는 `flutter create`가 ios·macos 항목을
  지워서 수동으로 3종(android·ios·macos) 복원 후 커밋.
