# 2026-09-05 작업 기록

## 개요
9월 5일 개선사항 5건(#45~#49)을 `feat/sep5-improvements` 단일 브랜치에서 구현.
기존 스택(#39·#40·#41) 머지 완료 확인 후 해당 이슈 3건 종료.

## 이슈·PR
| 이슈 | PR | 내용 | 상태 |
|------|----|------|------|
| #39·#40·#41 | #42·#43·#44 | 스택 머지 확인 (`origin/main` = `5592561`) 후 이슈 종료 + 종료 코멘트 | 완료 |
| #45 | #50 (예정) | 🧭 하단 탭 개편 (`eco`/`grass`/`settings` + 말씨/정원/설정) | 구현 완료 |
| #46 | #50 (예정) | 🌱 심자마자 명언 공개 + 명언 아래 성장 에셋 (`plantSeed` 즉시 공개, `_restoreRevealedQuote` 성장 중 복원) | 구현 완료 |
| #47 | #50 (예정) | ⚙️ 화면 모드(라이트/다크/시스템) + 기본 08:00 (`AppSettings.themeMode`, `app.dart` Consumer 적용) | 구현 완료 |
| #48 | #50 (예정) | 🧺 보관 읽기전용 (`FruitReviewSheet.readOnly`, 저장 UI 제거) | 구현 완료 |
| #49 | #50 (예정) | 🧹 3탭 상단 타이틀 삭제 (AppBar 제거 + SafeArea) | 구현 완료 |

## 결정 사항
- 5건을 상호 의존(동일 화면군 수정) 사유로 단일 브랜치·단일 PR로 처리, 본문에 `Closes #45~#49`.
- 화면 모드는 3안(라이트/다크/시스템, 기본 시스템 유지)으로 제공 — 기존 `ThemeMode.system` 동작 보존.
- 말씨 탭은 다크 고정 유지, 모드 설정의 적용 대상에서 제외.
- 보관 `ArchiveProvider.updateReview()`는 public API로 유지 (UI에서만 호출 제거).
- 명세 갱신: `model_spec` §4.9~§4.11, `feature_spec` §1~§3·§5, `convention` §2·§4.

## 검증
- `flutter analyze`: No issues found.
- `flutter test`: 61개 전부 통과 (기존 57 + 신규·수정 4).

## 잔여 작업
- PR #50 생성 후 개발자 검토/승인 (머지는 개발자만).
- 성장 에셋 17개 확보 시 적용 (기존 잔여).
- `#45~#49` 머지 후 이슈 자동 종료 확인.

## #51 성장 화면 레이아웃 (`feat/growth-layout`, 진행 중)
- `_QuoteBlock`(명언+저자) 분리 — 성장/완성 공용, 잠금 화면 재사용 전제.
- 성장 중: 명언 2/3 + 성장 에셋 1/3 (`Expanded` flex 2:1).
- 완성: 명언 2/3 + 열매 이미지 1/3 (`fruitImage` 재사용).
- 위젯 테스트에 flex 비율·열매 이미지 검증 추가.

## #54 테마 불일치 + 해시태그 제외 (`fix/sep5-theme-and-hashtag`, 진행 중)
- 원인: 기본 명언 4개가 4테마만 커버 → 나머지 테마는 폴백으로 엉뚱한 테마 확정·수확.
- 수정: 기본 명언 7개로 확충(`happiness`/`health`/`wisdom` 추가).
- 회귀 테스트: 전 테마 커버리지 검증 추가, 폴백 테스트는 미존재 테마 기준으로 수정.
- 해시태그 제외: `Quote.tags` 제거 + 시드 `tags:` 제거 + 명세(`model_spec` §4.1·§4.5, `convention` §2) 갱신.

## #56·#57 라이트 열매색 + 성장 미니멀 (`feat/light-fruit-minimal-growth`, 진행 중)
- `ThemeAssets.cellColor(theme, brightness)`: 라이트 밝은 7종 추가, 정원 잔디에 적용 (말씨 탭 제외).
- 성장 화면: 단계·안내 문구와 도트 삭제, 에셋 이미지만 유지.
- 테스트: 라이트/다크 상이 검증 + 성장 문구 제거 검증 (총 63개).

## #59·#60 니어블랙 + 탭 확대 (`feat/seed-abyss-big-nav`, PR #58 위 스택, 진행 중)
- `AppTheme.abyss`(`0xFF0A0C11`) 추가, 말씨 탭 배경에 적용.
- 하단 탭: 아이콘 30·라벨 12.5·상단 패딩 8 (테마 라벨 스타일도 12.5로 통일).
- 테스트: 말씨 탭 배경색 검증 추가.

## #62 심어도 잔디가 안 생김 (`fix/session-refresh`, PR #61 위 스택, 진행 중)
- 원인 2중 staleness: (1) `enableAutoRefresh: false`라 세션 중 성장 갱신 없음,
  (2) `ArchiveProvider.load()` 시작 시 1회라 세션 중 수확분 미반영.
- 수정: 15분 타이머 활성화 + 말씨/정원 탭 진입 시 갱신
  (`StatefulWidget` 전환, 첫 프레임 이후 호출).
- 테스트: 진입 시 reload/refresh 회귀 테스트 2개 추가 (총 65개).

## #64·#65 디버그 5초 성장 + 후기 후 잔디 (`feat/review-gated-grass`, 스택 팁에서 분기, 진행 중)
- `Seed.stageInterval`: 디버그 5초 / 릴리즈 2시간 (`kDebugMode` 가드).
  `SeedProvider.refreshInterval`: 디버그 5초 / 릴리즈 15분.
- `Fruit.isReviewed` (후기 또는 별점) + `ArchiveProvider.plantedFruits/plantedByDateKey` —
  잔디 그리드·개수·빈 상태가 심어진 기준.
- 테스트: 5초 간격 단계·이월 수정, 커버리지·게이트 회귀 추가 (총 68개).

## #67 과일별 성장 에셋 35종 (`assets/fruit-growth-stages`, PR #66 위 스택, 진행 중)
- 0단계 씨앗 이미지, 1~5단계 `<이름>-<n>.png` 매핑. 혼합 전략(17개) 폐기.
- 테스트: 매핑 검증 갱신.
