# 2026-09-15 작업 기록

## 세션 1 (오후)

### 개요
#173 도트 에셋 고해상도 교체 (방법 1: nearest-neighbor 4x 업스케일, 개발자 확정).
49개 PNG 일괄 교체 + `model_spec.md` §4.11 수치 갱신. analyze clean, test 207개 통과.

### 한 일
- `assets/hd-png-4x` 브랜치에서 `assets/images/*.png` 49개 4x 업스케일 (PIL NEAREST, `optimize=True`)
  - 씨앗 75px → 300px, 성장 170px → 680px, 열매 150px → 600px
  - 용량 1.49MB → 1.68MB (+13%)
  - 픽셀 검증: 49개 전부 HEAD 원본과 픽셀 단위 일치하는 정확한 4x (번짐 없음)
  - `screenshots/`는 온보딩용 실스크린샷이라 제외
- `grape_seed`는 원본 150px이 75px의 정확한 2x가 아니라 600px(150×4) 유지
  (75px로 내리면 픽셀 손실, 목표 300px+는 충족 — PR 본문에 명기)
- `docs/context/model_spec.md` §4.11에 에셋 해상도 기록 추가

### 검증
- `flutter analyze`: No issues found
- `flutter test`: 207개 전부 통과

### 이슈·PR
| 이슈 | PR | 내용 | 상태 |
|------|----|------|------|
| #173 | → `release/1.0.3` | 도트 에셋 4x 고해상도 교체 | PR 생성 예정 |

### 잔여 작업
- `release/1.0.3` 브랜치 생성 (버전 상향 1.0.3+4 + 출시노트) 후 기능 PR (`base=release/1.0.3`, `Closes #173`)
- v1.0.3 마일스톤 나머지 이슈 순차 처리
- 미커밋 잔여물: `.dart_tool/`, `macos/...Registrant.swift` (환경 부산물, unstaged 유지),
  `assets/docs/proverbs.csv`, `docs/quotes/proverbs_review.md` (untracked, 손대지 않음)

## 세션 2 (저녁)

### 개요
#173 후속: 흰 fringe 제거 + 검정 테두리. 모두 `assets/hd-png-4x`에 커밋·푸시 (PR #189 자동 반영).

### 한 일
- White descreen: 밝기>150 반투명 픽셀의 흰 매트를 역혼합 (11개 파일 1248픽셀, 알파 유지).
  진짜 흰색·내부 하이라이트·어두운 AA는 손대지 않음
- 검정 테두리: 1x 그리드에서 8-이웃 팽창 1px을 아트 바깥에만 추가 (49개 전부).
  씨앗 308px(77×4) · 성장 688px(172×4) · 열매 608px(152×4), 아트 픽셀 100% 보존
- 구현 중 실수: 1차 시도에서 팽창 루프가 제자리 읽기·쓰기로 배경 전체를 검게 채움 →
  즉시 `git checkout`으로 되돌리고 2-pass(스냅샷 후 적용)로 재실행. 검증(투명비율·림 2~8%·4x4 블록 균일·다크/라이트 합성 육안) 후 커밋
- `model_spec.md` §4.11에 테두리·최종 해상도 반영

### 검증
- `flutter test`: 207개 전부 통과 (이 브랜치 기준)

## 세션 3 (저녁 — v1.0.3 기능 웨이브)

### 개요
`release/1.0.3` 파고 신규 이슈 4건(#191/#195/#196/#192) 스케일 순 처리 + 마일스톤 정리.
전부 `base=release/1.0.3` PR로 머지됨.

### 한 일
- #152 → v1.0.4, #153 → v1.1.0 마일스톤 이동 (개발자 결정)
- #191 심겨진 씨앗 종류 표시 → PR #197 (성장·완성 1줄, 209개 통과)
- #195 정원 심김 1회성 팝 (`PlantCell`, 7일 윈도우) → PR #198 (209개 통과)
- #196 자정 배달 게이트 (배달 시각·수확 12시간 쿨다운, `seedTimeLoader` 설정 연동) → PR #199 (214개 통과, model_spec·feature_spec 반영)
- #192 업그레이드 유도 (`upgrader` ^13.7.0 한글 안내, `updateCheckEnabled` 테스트 seam) → PR #200 (209개 통과)
  - 표준 방식 조사: Android Play Core 인앱 업데이트 vs iOS 스토어 이동, 크로스플랫폼은 `upgrader` 채택 (context7 확인)

### 검증
- 각 PR마다 `flutter analyze` clean + 전체 스위트 통과

## 세션 4 (밤 — 애니메이션 반복 + 게이트 디버깅)

### 개요
성장 연출 3단 변천 (회전 → 분리 렌더 → 맥박) + 게이트 실기기 디버깅 2건.

### 한 일
- #154 흔들림(회전) → PR #190. 흙 동반 회전 지적 → #205 분리 렌더 PR #206 작성했으나
  경계선 노출로 폐기 (PR 닫음, 이슈 완료 처리)
- 전단(shear) 시도 후 최종 맥박 연출(1.2초 두근두근, 하단 중앙 고정)로 확정 → PR #209 (#207 플래시 방지 `seedKey` 포함)
- #201 글씨 상한(1.0~1.2) + 온보딩 단어 줄바꿈 + 컨벤션 §8 → PR #202
  - 원인: Android 시스템 글씨가 textScaler로 그대로 들어옴 (폰트 문제 아님)
- #196 실측 이슈: 디버그 +1시간에 게이트 stale → #203 (`debugAdvanceTime` 등 갱신) → PR #204
- 게이트 문구 교체 (`아직 배달시간이 되지 않았어요!` 등) → PR #214
- #210 축하 연출 (비 3초 + 팝, 후기 기준으로 변경) → PR #211
- #212 초기화 시 수확 기록 삭제 + 미래 수확 쿨다운 방어 → PR #213
  - 원인: 초기화時 fruits 잔류 + 미래 수확 음수 경과 영구 차단 (11월까지 밀리던 현상)

### 검증
- 각 PR마다 `flutter analyze` clean + 전체 스위트 통과 (최종 230개)

## 세션 5 (밤 — 1.0.3 릴리스 + 전역 스킬 + 1.0.4 파기)

### 한 일
- 출시노트 `docs/releases/1.0.3.md` 확정 (PR 12개 매핑 + 복붙용, 배달 시각 일반화 수정)
- 릴리스 PR #215 (`release/1.0.3` → `main`) 생성 → 개발자 머지 확인
- 완료 이슈 12개 `completed`로 닫음 (#173 #154 #191 #195 #196 #192 #201 #203 #207 #208 #210 #212)
- 태그 `v1.0.3` → `origin/main` 푸시 + GitHub Release 발행 (출시노트 포함)
- v1.0.3 마일스톤 open 0 확인
- 전역 스킬 `test-record` 신규 (`~/.config/opencode/skills/test-record/`)
  - `flutter test/analyze` 실행을 브랜치·커밋·명령어 키로 기록, clean 통과 커밋은 재실행 생략
  - dirty·실패 기록은 건너뛰기 근거 제외, 120일 정리. 격리 자가 테스트 통과
- `release/1.0.4` 파기 (버전 1.0.4+5 + 출시노트 stub, 마일스톤 #152 #139)

### 검증
- `flutter analyze`: No issues found / `flutter test`: 230개 통과 (test-record 기록)

### 잔여 작업
- #152 명언 크롤링 (속담 리뷰 100건 중 88건 후보, untracked `proverbs.csv`·`proverbs_review.md` 정리 필요)
- #139 위젯 (범위 확정 필요)
- 미커밋 잔여물: `.dart_tool/`, `macos/...Registrant.swift` (환경 부산물),
  `assets/docs/proverbs.csv`, `docs/quotes/proverbs_review.md` (untracked)
