# 2026-09-06 작업 기록

## 개요
9월 6일 작업 13건. 스택 랜딩(#111) 이후 열린 이슈를 순서대로 처리하고
`main`에 전량 머지했다. 테스트 101 → 135개, analyze clean으로 종료.

## 이슈·PR (모두 Closed/Merged, #129 제외)
| 이슈 | PR | 내용 | 상태 |
|------|----|------|------|
| #109 | #112 | 완성 후 날짜 이동 + 일자 변경선 (전일 작업분, 머지 확인) | 머지됨 |
| — | #113 (신규) | 날짜 이월 열매 잔디 미표시 (재현 테스트 S1 통과/S2 실패로 원인 확정) | — |
| #108 | #114 | 열매비 on/off 설정 (`AppSettings.fruitRainEnabled`) | 머지됨 |
| #115 (신규) | #116 | 디버그 시간 이동을 앱 공용 시계로 (`DebugClock`, `+1시간` 추가) | 머지됨 |
| #113 | #118 | 이월 미후기 열매 폐기 + 후기 마감 정책 (자동수확 폐기) | 머지됨 |
| #117 (신규) | #119 | 빨강·분홍 구분 + 오늘 테두리 테마색 (+ 포도 씨앗 에셋 교체) | 머지됨 |
| #120 (신규) | #121 | 관계 테마 보라색 (파랑과 구분, 문서 표기 `보라 씨앗` 통일) | 머지됨 |
| #122 (신규) | #127 | 로컬 지속화 (`SharedPreferences` + JSON, 재실행 복원) | 머지됨 |
| #123 (신규) | #124 | 명언 에셋 연동 + 큐레이션·엄선 (719→108件) + 명언별 출처 버튼 | 머지됨 |
| #125·#126 (신규) | #128 | 오늘 테두리 전용 청록 + 오늘로 이동 버튼 (올해 보기 중앙 복귀 포함) | 머지됨 |
| #129 (신규) | — | 그날의 명언 직접 작성 (설계 확정 후 착수) | **열림** |

## 주요 결정 사항
- 백엔드: 로컬 저장 채택 (Firebase는 동기화·공유 수요 시 별도 이슈). 명언은 번들, 개인 기록은 단말 저장.
- #109 자동수확 → #113 폐기 정책으로 대체 (다음날 씨앗 도착까지 미후기면 폐기).
- #117 오늘 테두리 테마색 → #125 전용 청록으로 방향 전환 (폴백 불필요).
- 명언 719件 → 108件 엄선 (극단·정보성·페미니즘·인용부호·생존인물 제외, 250자 초과 단축).
  peace 7·happiness 11·health 12는 원천 부족 — 추가 수집 필요.
- 출처는 정원 푸터에서 명언별 버튼(저자줄 오른쪽 구석)으로 전환. `Quote.source` + `Fruit.source` 스냅샷.
- `#126` 올해 보기에서도 중앙 복귀 (당초 비활성화안 변경).

## 검증
- `flutter analyze`: No issues found.
- `flutter test`: 135개 전부 통과.
- `main` = `ef0743a` (PR #128 머지).

## 잔여 작업
- #129 그날의 명언 직접 작성 (진입점·테마 규칙·수정/삭제 정책 확정 후 착수).
- peace·happiness·health 명언 추가 수집 (별도 이슈화 검토).
- 미커밋 잔여물: `.dart_tool/` 수정분 (pub 부산물, 기존 방침대로 unstaged 유지),
  `macos/Flutter/GeneratedPluginRegistrant.swift` (빌드 부산물, 미커밋).
